apiVersion: v1
kind: Service
metadata:
  name: {{ include "info-app-backend.fullname" . }}
  labels:
    {{- include "info-app-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "info-app-backend.selectorLabels" . | nindent 4 }}
